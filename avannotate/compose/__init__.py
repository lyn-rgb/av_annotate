"""Assembling the deliverable: ten stages' records into one document.

No model runs here.  Everything in this package decides what the output file
says, which is why it is pure and tested -- the failure mode is not an exception
but a document that reads as finished and is wrong.
"""
